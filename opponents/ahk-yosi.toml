# ahk-yosi
#
# Both values come from THEM. `group_id` must equal what their handshake
# declares, or the filer's rename never fires and our emitted report is keyed
# by a placeholder instead of their name.
url      = "https://reports-lens-chancellor-vitamins.trycloudflare.com/mcp"
group_id = "ahk-yosi"
name     = "ahk-yosi"
notes    = """

ONE PROCESS, ONE DOOR (corrected by them 2026-08-20, unprompted). Their earlier
"two processes, two ports, two hostnames" was inferred from their runbook and
submission split, not checked against their code. A series is played by a single
process that alternates roles at each boundary, so BOTH cards carry the same URL
and our per-window retarget is a no-op that always lands on the process holding
the window. Do not put the old second hostname back: under one process it is an
address with nothing behind it, and three of six windows would go there.

This is the shape of 2026-08-02. One process, one inbox, a peer retrying into it
— 58 inbound negotiates, two games at once over one game state, five mini-games
at 0-0 technical. `net/match_gate.py` exists because of that day and has never
been tested against the architecture that caused it. Expect our cop to be
handshaking window N+1 at their single door while their process is mid-window N;
they say their negotiate always answers ok and queues to the boundary, which is
what should make it survivable. Watch `handshake.delivered_in_reply` and
`inbox.absorbed`.

COUNTED SERIES PLAYED 2026-08-21 — **tied 75-75, 3-3**, our counted #6, archived
at `matches/ahk-yosi-COUNTED-20260821T183302Z`. All six mini-games ended in a
capture and every capture went to the side holding the cop, so the series is a
dead heat by construction. Digest `5bb96826…` confirmed both sides, six of six
audits `log_verified: true`. **One counted per pairing — do not play them again
for the record.**

Two warm-ups preceded it on 2026-08-21 (30-90 both times, before the barrier
fix); the 2026-08-02 meeting was void and both teams treat 2026-08-21 as the
first meeting.

RULE 1 CAVEAT, RECORDED BECAUSE IT IS THEIRS AND NOT OURS. They play a series
from ONE process alternating roles, which does not satisfy Appendix ה Table 7
rule 1. We raised it; they offered to split before anything counted; the series
was played on the understanding that we changed nothing on our side, and we did
not — only this card moved. We filed what happened and claim nothing beyond it.
If the rule is read strictly the exposure is on their side of the report.
their THIEF door — use this card when running najamjad-cop.

Yosef Shanaa + partner (ids 213314859, 325811255). Replied 2026-08-20 with the
most thorough conformance audit any opponent has sent us: both our vectors
re-derived through their own code, five defects on THEIR side volunteered
unprompted, and two real errors found in our own terms document (a counted count
that disagreed with itself, and the stale scent table). Both of ours are fixed.

AGENT EMAIL: apexmediamind@gmail.com — NOT the yosefshanaa@gmail.com in their
reply block. Naji confirmed the former is the live one on 2026-08-20; the reply's
own §6 is wrong about it. Practice reports go to apexmediamind@gmail.com.

SCENT — THIS PAIRING IS A2, `subtractive_chebyshev_v1`. We are currently
configured for `book` (anrbj666). **Set `pheromones.pheromone_model` back to
"reference" in config/game.json before dialling them, in BOTH repos.** Their
reason is worth recording: their book lock document hashes ea7225f5… where the
kit's (and ours) is 934c220d… — same physics, different document — and their
handshake treats a scent-hash difference as a refusal, not a warning. So A1
would not start a game between us. Their subtractive digest is identical to
ours because both sides adopted the kit's registered document verbatim.

ENDPOINTS ROTATE. Quick tunnels, not named ones, so both URLs change on restart
and they send them immediately before dialling. The url below is a deliberate
placeholder that fails loudly — replace it with what they send, do not dial the
2026-08-02 Cloud Run address that used to be here.

THE PRIOR MEETING WENT BADLY, AND NOT THEIR FAULT ALONE. 2026-08-02 warm-up:
we logged 58 inbound negotiates across a six-game series because their peer
retried our endpoint on a loop while we dialled theirs, so two games ran at once
over one inbox and one game state. All five completed mini-games scored 0-0
technical. `net/match_gate.py` exists because of that day. They count us as a
first meeting, which is right for counted purposes — that one was void.

FOUR THINGS TO SETTLE BEFORE A COUNTED SERIES:
  * **They advance past a failed window; we re-offer it.** They asked us to
    pick and said ours is the safer build. Pick re-offer — advancing at
    different rates is precisely what killed the anrbj666 series.
  * **Their police files an enclosure on its own turn** rather than waiting for
    the thief's concession. Rule 47 wants the thief to confirm; an unconfirmed
    capture voids for both (rules 33-35). They offered to switch it off — take
    that offer.
  * **submit_audit envelope**: they send five fields, our §5 says exactly three,
    our §3.1 says we tolerate extras. Both are true from different ends (we
    tolerate on receive; a reference peer may not). Tell them the strict three —
    they said they lose nothing, since each record's payload carries its own
    sub_game.
  * **Their patience is 180 s opening / 90 s re-handshake**, against our 16.7
    minutes. They are the side that gives up first and asked us to name a
    number. Name one at or above ours.

Their repos are private and 404, same as ours; they said so unprompted and are
willing to open both before a counted series if we are.
"""

# Sent 2026-08-20 in their reply. Both repos 404 (private), so these cannot be
# inspected — but `audit_opponent.py` compares them against the `github_commit`
# their step-0 declares, which works regardless of visibility. They also gave a
# single workspace commit, 9ebdc2aa204909ea122be59caf7eea2182ddc303, that both
# role repos are generated from by a sync script.
[armed_commits]
cop   = "c2ac57ba32447c5ff73115820dbbbb5ff7a105f2"
thief = "c2ac57ba32447c5ff73115820dbbbb5ff7a105f2"
