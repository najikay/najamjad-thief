# moaamoha-cop
#
# Both values come from THEM. `group_id` must equal what their handshake
# declares, or the filer's rename never fires and our emitted report is keyed
# by a placeholder instead of their name.
url      = "https://calm-lantern-322.local.dev/mcp"
group_id = "MOAAMOHA"
name     = "moaamoha-cop"
notes    = """
Moaawiyah Hajajrah, Mohamed Selawe. First counted series for them, so
first_meeting is true and the diversity reward rides on whoever wins.

TWO processes, one per role, on two different hosts:
  their cop    https://calm-lantern-322.local.dev/mcp          (Cloudflare)
  their thief  https://bright-harbor-604.local.dev/mcp             (Cloudflare)
Both resolve. `url` above is their COP door. Use this card when WE open as thief, i.e. when
running najamjad-thief: they are police in sub-games 1/3/5, so window 1 dials
their cop and the per-role retarget swaps to their thief door for 2/4/6. Use
`moaamoha` (their thief door) when running najamjad-cop instead — picking the
wrong card sends window 1 to a process that is not expecting it.

agent email  moaawiyah.haj@gmail.com
repos        github.com/Moaawiyah/police-agent · github.com/Moaawiyah/Ai_thief

SCENT MODEL - settle before dialling. They declare a Gaussian kernel and treat
kernel shape as unsigned. Rule 23 locks the model, and our declared
scent_model_sha256 81ebee59640e80eae8ca9ee5f86abd26e7edf5cdbb27d15925cb6ee45ca6ddf4
is scent_model:subtractive_chebyshev_v1. They match it or we do not play; we
are not carrying a second convention. Formula for them:
  weight = max(0, 1 - chebyshev(dr, dc) / 3)      # 5x5, half = 2
  value  = round(0.9 * weight, 2)                 # rings 0.90 / 0.60 / 0.30

Snapshot timing needs nothing: they decay-deposit-send, we deposit-send-decay,
and both put peak 0.9 on the wire.
"""

# Awaiting their reply. Full 40 characters, one per repo - their cop and thief
# are separate checkouts, so the pair can differ. `scripts/audit_opponent.py`
# compares these per role against the `github_commit` their step-0 declares.
[armed_commits]
cop   = ""
thief = ""
