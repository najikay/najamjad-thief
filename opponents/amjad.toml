# amjad
#
# Both values come from THEM. `group_id` must equal what their handshake
# declares, or the filer's rename never fires and our emitted report is keyed
# by a placeholder instead of their name.
url      = "https://focused-wheel-toolbox-housewives.trycloudflare.com/mcp"
group_id = "najamjad-b"
name     = "amjad"
notes    = "Quick tunnel, rotates overnight — re-ask before every match. His server now returns ok+accepted on every reply including rejections; we read 'accepted', not 'ok', so that change is inert for us. Same team as us: can never be a counted match (rule 31), which makes him the free sparring partner for full-strength rehearsals."
