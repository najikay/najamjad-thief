# amjad
#
# Both values come from THEM. `group_id` must equal what their handshake
# declares, or the filer's rename never fires and our emitted report is keyed
# by a placeholder instead of their name.
url      = "https://speakers-worthy-communication-costume.trycloudflare.com/mcp"
group_id = "najamjad-b"
name     = "amjad"
notes    = "Teammate — can NEVER be counted (rule 31), so the only partner we rehearse at FULL strength. Quick tunnel, rotates every restart: re-ask before every session. Merged to 1c96589/650f9ea. He opens as thief and dials our cop, so we run najamjad-cop."
