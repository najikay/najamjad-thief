# vibecode
#
# Both values come from THEM. `group_id` must equal what their handshake
# declares, or the filer's rename never fires and our emitted report is keyed
# by a placeholder instead of their name.
url      = "http://62.56.220.143:61223/mcp"
group_id = "vibecode"
name     = "vibecode"
notes    = "Ron Marom, Amit Kuperminz. TWO processes, TWO ports (cop :61224, thief :61223), static IP, no tunnel — the first genuinely role-split opponent, which the pre-handshake retarget exists for. We open as COP (najamjad cop in 1/3/5), so sub-game 1 dials their THIEF door :61223 and the retarget swaps per sub-game. No LLM. Terms byte-identical: game_uid eb6ddfa3 verified both ways."

# What they told us in writing they would play. Awaiting their reply of
# 2026-08-14; paste the full 40 characters here when it arrives.
#
# Leave empty rather than pasting a prefix. An unset value is skipped; a wrong
# one raises a rule-53 finding against an honest team, and `c5a8b9d` in the
# 2026-08-13 handoff was recorded as 7 characters and cannot be compared.
#
# For reference, what their agents actually DECLARED in the 2026-08-13 friendly
# (read off their own step-0 records by `scripts/audit_opponent.py`):
#
#   their cop    1ab98583e69b7046a48a0e14f3d9b5691d03062d
#   their thief  ee853d79f0fed2fb4e947ba9c246c51d1ab06470
#
# Note their two processes run two different checkouts, and that their cop had
# already moved past the `c5a8b9d` we were told. These are evidence of what they
# ran last time, NOT a substitute for what they tell us they will run next time
# — copying them in here would make the check compare their archive to itself.
[armed_commits]
cop   = ""
thief = ""
