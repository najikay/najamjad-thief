# Handoff — 2026-08-14

Supersedes `HANDOFF-2026-08-13.md`, which is four commits stale. Written to be
read cold: every claim below is either verifiable in the repository or flagged as
an open question. Where a previous session asserted something and later measured
it to be false, both are recorded — the corrections are the most useful part.

---

## 1. State

| | |
|---|---|
| **Deadline** | **2026-08-20** (postponed cohort-wide; the 12/08 in `project info and links.txt` is superseded) |
| **Counted series** | **2 — rule 31 pass threshold met** |
| Head, cop | `27d639fb9d0688f9a0f844657b5a262d7c3378b5` |
| Head, thief | `329f88f524f4c2e462a74040c855f0aa777bd0fb` |
| Pushed | **yes, both** |
| Armed | `sandbagged` + `draft` (friendly posture) |
| `practice.redirect_to` | `najikayal4@gmail.com` (our own) |
| Workspaces | empty — nothing to overwrite |

### Counted ledger

| # | Opponent | Date | Result | Archive |
|---|---|---|---|---|
| 1 | uoh-ay26 | 2026-08-08 | won 90-30, 6-0 | `matches/uoh-ay26-counted-20260812T111029Z` |
| 2 | imreeyal | 2026-08-13 | won 90-30, 6-0 | `matches/imreeyal-COUNTED-20260813T164811Z` |

The count is **derived, never typed**: `workspace/counted_games.json` is written
at settlement and the handshake declaration reads it. Do not hand-edit.

**The bonus is not a deliverable.** The course text: a bonus of up to 10 points is
added *"according to the grade of the bonus game **submitted as part of
Assignment 06**, for those who submitted"*. Past tense. Nothing here earns it.

---

## 2. Next up — vibecode

Their opening message is `greeting-new-opponent.md` at the project root. A
friendly is owed; last night's attempt reached 4 of 6 and was **filed by neither
side**, so it costs nothing.

```
group_id:     vibecode
members:      Ron Marom, Amit Kuperminz
repos:        github.com/AmitKuper/vibecode-cop · vibecode-thief
MCP cop:      http://62.56.220.143:61224/mcp
MCP thief:    http://62.56.220.143:61223/mcp
llm_model:    none
armed commit: cop c5a8b9d / thief ee853d7   (cop bumped after their fix)
counted:      3 claimed (anrbj666, imreeyal, uoh-sqak)
```

**They are our first genuinely role-split opponent** — two OS processes on two
ports, static IP, no tunnel. Everyone before served every role from one address.

**We open as cop**, so run **`najamjad-cop`** (port 8802, `cop.4laboratory.com`),
and sub-game 1 dials their **thief** door `:61223`. The card holds that. The
per-sub-game swap to `:61224` comes from the endpoints their handshake declares.

Verified before a card was written: their published `game_uid`
`eb6ddfa3-6dde-836d-78d5-8c4e8e2d1372` reproduces exactly from **our** flat
terms. Since the uid hashes those terms plus the sorted pair, that is proof both
sides sign the same object. Their whole-file `config_sha256` differs from our
flat-terms `a284082d…` by construction and **cannot refuse anything** — we
compare the extraction, not the file.

### Settled with them in writing

Scent `subtractive_chebyshev_v1` (hash matches); reference-v3 wire shape;
`info_mode: belief`; thief-first; `game_id` the sorted pair; diversity reward
winner-only with `total_score` the pure sub-game sum; friendly reports to each
other, counted to the lecturer alone. They accepted our single-process topology
and our alias superset — see §5.1.

### What happened last night, and whose fault each half was

Sub-games 3 and 5 never started. **Their diagnosis was wrong and ours was right,
and they conceded it from their own logs.** They claimed we never re-dialled
their thief door; our trace showed the retarget firing **one millisecond** after
each boundary and 9 handshake attempts against `:61223`, all **read** timeouts —
meaning their socket accepted the connection and never answered. Their log later
confirmed 78 POSTs to `:61223` up to our session close and zero after: their
streamable-HTTP session layer **wedged after our DELETE**. Fixed on their side by
probing their own door before each window and rebuilding the stack on silence.

We lost the four played sub-games 20-70. **We were sandbagged** — no barriers, a
3× claim bar, the weak thief policy — so it says little about full strength.
Their cop captured at step 12 in all three thief games with a barrier at step 10,
and g02/g04 were **byte-identical**, which is the signature of honest determinism
(they run no LLM) rather than anything suspicious.

---

## 3. Running a match

Run **one repo** for the whole series; it alternates roles internally. Which repo
you run decides only which side you *open* on. The thief opens every sub-game.

```bash
# 1. card (writes into BOTH repos)
uv run python scripts/match_day.py card --team <name> --url <their-url> \
    --group-id <their-exact-group-id>

# 2. arm
uv run python scripts/match_day.py warmup     # sandbagged + draft  <- friendlies
uv run python scripts/match_day.py counted    # full + send         <- counted only

# 3. tunnel, own terminal, left running (8802 cop / 8801 thief)
cloudflared tunnel run --url http://127.0.0.1:8802 najamjad-cop

# 4. preflight, then play
uv run najamjad-cop preflight --opponent <name> --practice
uv run najamjad-cop match --opponent <name> --no-tunnel --practice \
    --talk --dashboard --dashboard-host 0.0.0.0

# counted: drop --practice and --dashboard-host
```

**`match_day.py archive --team <label>` before every rerun.** `game_id`,
`game_uid` and every filename derive from the two group ids and the terms, so a
second series against the same opponent silently overwrites the first.

### Signs that look like failures and are not

| Sign | Meaning |
|---|---|
| Long silence between sub-games | A peer rebinding a fresh process. `handshake_retries = 8` ≈ 6 min. **Do not Ctrl-C** |
| `client.session_dropped` | The §4.2 fix firing, once per boundary, before the handshake |
| `Session termination failed: 502` | Harmless SDK warning from that drop |
| `502` on our door with no agent up | Tunnel alive, agent not started. `406` is healthy |
| Dashboard unreachable from Windows in a **counted** run | Deliberate — counted binds loopback (rules 8-9). Open it inside WSL |

---

## 4. What has been fixed, and what each cost

Every one was found by an opponent's review or by measuring something we had
asserted. **The pattern is the lesson.**

| # | Defect | Cost |
|---|---|---|
| 4.1 | **Capture claim never reached the belief.** `cop_sighting.from_claim` written, tested, **no caller** — only `from_barrier` was wired. We parsed the cop's exact declared cell to answer it, then discarded it. Belief peak sat at the diffusion default whatever the cop claimed | Every claim of every series. 33 in the vibecode games alone |
| 4.2 | **Dead socket at each sub-game boundary.** One MCP session held across the series; a peer with a fresh process per sub-game left it attached to a dead process. Calls hang to the cap while `curl` reads a healthy 406 | A full friendly |
| 4.3 | **Session-guard race.** Guard bound *after* the handshake; a fast opener landed in the gap and was refused as the wrong sender | Sub-games 2, 4, 6 — three nights |
| 4.4 | **Role-split retarget.** Per-role endpoint adopted only after the handshake | Would have broken every even sub-game vs vibecode |
| 4.5 | **Agreement digest matched nobody.** We hashed `{game_id, game_uid, groups, sub_games}` compact; the reference signs `{game_id, aggregate, sub_games}` **spaced** | Counted #1 — see §5.1 |
| 4.6 | **`counted_games_played`.** We declared only `counted_matches_played`; league readers look for the other spelling, so an honest opponent's report carried `najamjad: 0` | A false count in someone else's file |
| 4.7 | **`winner_group` never relabelled** from the tracker's `"them"` placeholder. **Only bites on a defeat** — five straight wins hid it | Would have self-contradicted our own artifact |
| 4.8 | **Cop had no strength dial.** Every "sandbagged" warm-up played our real cop policy in 3 of 6 sub-games | Our policy, handed to teams we may meet again |
| 4.9 | **Two preflight checks reported green without checking** — `--check` passed a revoked Gmail token; `tunnel_check` echoed the configured hostname | A dead mail path, undetected |

**Measured caveat on 4.8:** across 49 position/peak pairs the two strength levels
pick a different *move* only 3 times. On a 7×7 board every sane pursuit walks at
the peak, so the weakening lives in barriers and the claim bar, not movement.

---

## 5. Open items

### 5.1 The uoh-ay26 digest — the one real risk
Both reports **agree on every game fact**: six sub-games' roles, results, winners,
scores, 90-30, 6-0, one shared `game_uid`, `confirmed: true` both sides. Only
`mutual_agreement.sha256` differs — and theirs reproduces **neither our old
construction nor the reference's**, so three implementations gave three answers.

Rule 35 voids a match for *contradictory reports*. These contradict on nothing.
**The book defines no remediation procedure** — verified, not assumed.

**Mitigation: play a third counted series.** At 3 counted, the worst reading of
uoh-ay26 still leaves 2 and above threshold. Entirely in our control; a
favourable ruling is not. vibecode is that third series. Also worth telling
uoh-ay26 what we found.

### 5.2 Receiver-side scent decay (parked deliberately)
imreeyal transmit **post-decay** and decay on receipt; we transmit **pre-decay**,
so ours nets exactly one decay in their field — correct. Theirs ages **twice** in
ours. Their suggested fix (skip our receipt decay) is **wrong for our code**: our
`absorb` max-merges, so without also replacing rather than merging, their trail
would never fade at all. Measured: with the extra decay our estimate tracks their
current cell; without it, it sticks to where they were first seen. **Do not ship
the naive version.**

### 5.3 Hints
`parse_locally` reads compass words. Across 102 imreeyal hints, **0 parsed**.
Their landmarks leak position strongly — "Hell's Kitchen" appeared 42 times and
every time they were in column 0-1 — and we discard all of it. Largest unused
signal. Fit it to more than one opponent before relying on it.

A place name containing a compass word ("East Village") parses as a direction
claim. Real, but it never fired against imreeyal.

### 5.4 A flaky test
`test_series_survives_a_stall.py::test_a_storm_costs_at_most_the_game_it_hits_
and_the_one_after` fails under CPU contention (both repos' suites in parallel),
passes 8/8 alone. Pre-existing, timing-based. Re-run rather than chase — but only
after confirming nothing else is red.

---

## 6. Conventions agreed in writing

- **`mutual_agreement.sha256`** = sha256 over `{game_id, aggregate, sub_games[
  sub_game_number, roles, result, winner_group, score]}`, sorted keys, raw UTF-8,
  **spaced separators**. Byte-equal on both sides. Reproduced against imreeyal.
- **`tokens_total_series`** is per-side: own spend, **zero** for the opponent.
- **Diversity reward** derived: counted AND first meeting AND this group won.
  Crowns whoever won. `total_score` stays the pure sub-game sum.
- **Mail:** body = the attachment's **exact bytes**. Subject:
  `Police-Thief series result: winner <group_id> (reported by <role>)`.
- **Friendly** reports to the opponent and ourselves, **never** the lecturer.
  **Counted** to `rmisegal+uoh26finalgame@gmail.com` **alone**.
- **T protocol:** both holding by T−10, curl at T (406 = ready), explicit "go"
  each side, then dial. Not 406 by T+30s → new T. Never debug inside a window.

---

## 7. Discipline that has already saved us

- **Freeze before declaring a hash.** `git status` empty, then `git rev-parse
  HEAD`, then send the **full 40 characters**. A team blocked their own
  submission over a 7-char hash.
- **Arming is sticky.** `counted` leaves `send` + `full` in *tracked* config.
  Disarm afterwards or the next warm-up plays at full strength and the next
  practice mails whoever `practice.redirect_to` names. Found in the wrong state
  once already.
- **`practice.redirect_to` is practice-mode-only.** A counted run ignores it and
  routes to the configured lecturer address. Verified, not assumed.
- **Archive before every rerun.**
- **Verify a finished series**: `replay` every log, the kit's
  `tools/check_artifacts.py --terms`, and compare against the opponent's file
  *before* anyone files.
- **The two-process harness writes to a `TemporaryDirectory` deleted on exit.**
  Its events are gone before you can read them — never use it to prove an event
  fires. Drive `MatchRunner` in an integration test instead.

---

## 8. The one habit worth keeping

**Five components this week existed, passed their own tests, and were never
called**: the artifact writer, the reconciler, the session guard, the thief's
strength dial, and `cop_sighting.from_claim`. Several more were properties we had
asserted in writing and never measured — the declared scent form, the agreement
digest construction, the count key name, two preflight checks, and "they send no
hints" (measured from *sealed records*, which structurally cannot carry hints in
their build).

Reading the call site and believing it is not verification. The question that
found all of them is:

> **Does this actually reach anything, and how would I know?**

Answer it by feeding the real thing through the real path and watching the output
change — not by reading the code and reasoning that it should.
